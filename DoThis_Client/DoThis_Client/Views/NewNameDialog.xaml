<Window x:Class="DoThis_Client.Views.NewNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New name" Height="200" Width="400"
        WindowStartupLocation="CenterScreen"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}" Icon="/Views/to-do-list.ico">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Enter a new name:" 
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>

        <TextBox x:Name="TaskNameTextBox" 
                 Grid.Row="1"
                 Margin="0,0,0,0"
                 materialDesign:HintAssist.Hint="Title"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="CANCEL" IsCancel="True" Margin="0,0,8,0" Style="{StaticResource MaterialDesignFlatButton}"/>
            <Button Content="OK" IsDefault="True" Click="Ok_Click" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>